/**
 * ═══════════════════════════════════════════════════════════════
 * 🤖 AKIRA BOT - ANA GİRİŞ NOKTASI
 * ═══════════════════════════════════════════════════════════════
 * 
 * Modern, ölçeklenebilir ve güvenli Discord bot altyapısı
 * 
 * @author Akira Team
 * @version 1.0.0
 * @license MIT
 */

// ─────────────────────────────────────────────────────────────
// 📦 GEREKLİ MODÜLLER
// ─────────────────────────────────────────────────────────────

const { Client, GatewayIntentBits, Partials } = require('discord.js');
const config = require('./src/config/botConfig');
const { Logger } = require('./src/utils');
const { loadCommands, loadEvents, setupErrorHandlers } = require('./src/handlers');
const database = require('./src/database/connection');

// ─────────────────────────────────────────────────────────────
// 🤖 DİSCORD CLIENT OLUŞTURMA
// ─────────────────────────────────────────────────────────────

/**
 * Intent'ler - Sadece gerekli olanlar aktif
 * 
 * Guilds: Sunucu bilgilerine erişim
 * GuildMembers: Üye olayları (join/leave)
 * GuildMessages: Mesaj olayları
 * GuildMessageReactions: Tepki olayları (opsiyonel)
 * MessageContent: Mesaj içeriği (prefix komutları için - şu an kullanılmıyor)
 */
const client = new Client({
    intents: [
        GatewayIntentBits.Guilds,
        GatewayIntentBits.GuildMembers,
        GatewayIntentBits.GuildMessages,
        GatewayIntentBits.GuildPresences // Presence bilgisi için (online/offline)
    ],
    partials: [
        Partials.Channel,
        Partials.GuildMember,
        Partials.User
    ],
    // Rate limit yönetimi
    rest: {
        timeout: 15000,
        retries: 3
    },
    // Sweep ayarları (memory optimizasyonu)
    sweepers: {
        messages: {
            interval: 300, // 5 dakikada bir
            lifetime: 600  // 10 dakikadan eski mesajları temizle
        }
    }
});

// ─────────────────────────────────────────────────────────────
// 🚀 BOT BAŞLATMA FONKSİYONU
// ─────────────────────────────────────────────────────────────

async function startBot() {
    try {
        Logger.info('Bot başlatılıyor...');

        // ─────────────────────────────────────────────────────────
        // 1️⃣ HATA YAKALAYICILARI AYARLA
        // ─────────────────────────────────────────────────────────
        setupErrorHandlers(client);

        // ─────────────────────────────────────────────────────────
        // 2️⃣ VERİTABANI BAĞLANTISI
        // ─────────────────────────────────────────────────────────
        Logger.info('MongoDB bağlantısı kuruluyor...');
        await database.connect();

        // ─────────────────────────────────────────────────────────
        // 3️⃣ KOMUTLARI YÜKLE
        // ─────────────────────────────────────────────────────────
        Logger.info('Komutlar yükleniyor...');
        await loadCommands(client);

        // ─────────────────────────────────────────────────────────
        // 4️⃣ EVENT'LERİ YÜKLE
        // ─────────────────────────────────────────────────────────
        Logger.info('Event\'ler yükleniyor...');
        await loadEvents(client);

        // ─────────────────────────────────────────────────────────
        // 5️⃣ DISCORD'A BAĞLAN
        // ─────────────────────────────────────────────────────────
        Logger.info('Discord\'a bağlanılıyor...');
        await client.login(config.bot.token);

    } catch (error) {
        Logger.error('Bot başlatılamadı:', error);
        process.exit(1);
    }
}

// ─────────────────────────────────────────────────────────────
// 🎬 BOTU BAŞLAT
// ─────────────────────────────────────────────────────────────

startBot();

// Export for potential testing
module.exports = { client };
