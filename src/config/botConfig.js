/**
 * ═══════════════════════════════════════════════════════════════
 * 🤖 AKIRA BOT - ANA KONFİGÜRASYON DOSYASI
 * ═══════════════════════════════════════════════════════════════
 * 
 * Bu dosya bot'un tüm yapılandırma ayarlarını içerir.
 * Ortam değişkenleri (.env) ile entegre çalışır.
 */

require('dotenv').config();

module.exports = {
    // ─────────────────────────────────────────────────────────────
    // 🔑 TEMEL AYARLAR
    // ─────────────────────────────────────────────────────────────
    bot: {
        token: process.env.BOT_TOKEN,
        clientId: process.env.CLIENT_ID,
        guildId: process.env.GUILD_ID,
        devMode: process.env.DEV_MODE === 'true'
    },

    // ─────────────────────────────────────────────────────────────
    // 👨‍💻 GELİŞTİRİCİ AYARLARI
    // ─────────────────────────────────────────────────────────────
    developers: {
        // Geliştirici ID'lerini array olarak parse et
        ids: process.env.DEVELOPER_IDS?.split(',').map(id => id.trim()) || [],
        
        // Geliştirici ayrıcalıkları
        privileges: {
            bypassCooldown: true,      // Cooldown'dan muaf
            bypassPermissions: false,   // Yetki kontrolünden muaf DEĞİL (güvenlik)
            accessDevCommands: true     // Developer komutlarına erişim
        }
    },

    // ─────────────────────────────────────────────────────────────
    // 🗄️ VERİTABANI AYARLARI
    // ─────────────────────────────────────────────────────────────
    database: {
        uri: process.env.MONGODB_URI || 'mongodb://localhost:27017/akira_bot',
        options: {
            maxPoolSize: 10,
            serverSelectionTimeoutMS: 5000,
            socketTimeoutMS: 45000
        }
    },

    // ─────────────────────────────────────────────────────────────
    // ⏱️ COOLDOWN AYARLARI
    // ─────────────────────────────────────────────────────────────
    cooldown: {
        defaultCooldown: 3,            // Varsayılan cooldown (saniye)
        maxCooldown: 300,              // Maksimum cooldown (5 dakika)
        useDatabase: true,             // MongoDB kullan (false = memory)
        cleanupInterval: 60000         // Temizlik aralığı (ms)
    },

    // ─────────────────────────────────────────────────────────────
    // 🎨 BOT DURUMU
    // ─────────────────────────────────────────────────────────────
    presence: {
        status: process.env.BOT_STATUS || 'online', // online, idle, dnd, invisible
        activity: {
            type: process.env.BOT_ACTIVITY_TYPE || 'WATCHING',
            name: process.env.BOT_ACTIVITY_NAME || 'Akira Sunucusu'
        }
    },

    // ─────────────────────────────────────────────────────────────
    // 📝 LOG AYARLARI
    // ─────────────────────────────────────────────────────────────
    logging: {
        webhookUrl: process.env.LOG_WEBHOOK_URL || null,
        enableConsole: true,
        enableWebhook: !!process.env.LOG_WEBHOOK_URL,
        levels: {
            info: true,
            warn: true,
            error: true,
            debug: process.env.DEV_MODE === 'true'
        }
    },

    // ─────────────────────────────────────────────────────────────
    // 🎨 EMBED RENKLERİ
    // ─────────────────────────────────────────────────────────────
    colors: {
        primary: 0x5865F2,    // Discord Blurple
        success: 0x57F287,    // Yeşil
        warning: 0xFEE75C,    // Sarı
        error: 0xED4245,      // Kırmızı
        info: 0x5865F2        // Mavi
    },

    // ─────────────────────────────────────────────────────────────
    // 📋 EMOJI'LER
    // ─────────────────────────────────────────────────────────────
    emojis: {
        success: '✅',
        error: '❌',
        warning: '⚠️',
        info: 'ℹ️',
        loading: '⏳',
        cooldown: '🕐',
        ban: '🔨',
        kick: '👢',
        mute: '🔇',
        warn: '⚠️'
    },

    // ─────────────────────────────────────────────────────────────
    // 📁 KOMUT KATEGORİLERİ
    // ─────────────────────────────────────────────────────────────
    categories: {
        admin: {
            name: 'Admin',
            emoji: '👑',
            description: 'Yönetici komutları'
        },
        moderation: {
            name: 'Moderasyon',
            emoji: '🛡️',
            description: 'Moderasyon komutları'
        },
        general: {
            name: 'Genel',
            emoji: '📋',
            description: 'Genel kullanım komutları'
        }
    }
};
